# This ConfigMap is generated by scripts/bootstrap.sh from the JSON files.
# To regenerate manually:
#   oc create configmap grafana-dashboards \
#     --from-file=ocp/monitoring/dashboards/mq-fleet-overview.json \
#     --from-file=ocp/monitoring/dashboards/bcl-gateway.json \
#     --from-file=ocp/monitoring/dashboards/migration-progress.json \
#     --from-file=ocp/monitoring/dashboards/agent-mesh.json \
#     -n mq-hackathon --dry-run=client -o yaml | oc apply -f -
#
# The bootstrap script handles this automatically.
